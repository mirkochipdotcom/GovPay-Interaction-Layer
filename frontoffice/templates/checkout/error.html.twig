{% extends 'base.html.twig' %}

{% block title %}Errore pagamento – {{ app_entity.name }}{% endblock %}

{% block content %}
  <div class="container-xxl py-5">
    <div class="row justify-content-center">
      <div class="col-lg-8">
        <div class="alert alert-danger" role="alert">
          <h1 class="h4 mb-2">Si è verificato un errore</h1>
          <p class="mb-0">Non è stato possibile completare l’operazione nel Checkout pagoPA. Puoi riprovare tra qualche minuto.</p>
        </div>

        {% if id_pendenza %}
          <p class="text-muted">Riferimento pendenza: <strong>{{ id_pendenza }}</strong></p>
        {% endif %}

        <div class="d-flex flex-wrap gap-3 mt-4">
          {% if spid_enabled and (not is_logged_in) %}
            <a class="btn btn-primary" href="{{ login_path }}">Accedi e riprova</a>
          {% else %}
            <a class="btn btn-primary" href="{{ detail_path }}">Torna alla pendenza</a>
          {% endif %}
          <a class="btn btn-outline-primary" href="/pendenze">Le mie pendenze</a>
          <a class="btn btn-outline-secondary" href="/">Torna alla home</a>
        </div>

        <div class="mt-4">
          <p class="mb-0">Se l’errore persiste, contattaci a <a href="mailto:{{ support_email }}">{{ support_email }}</a>.</p>
        </div>
      </div>
    </div>
  </div>
{% endblock %}
