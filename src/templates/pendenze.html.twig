{% extends 'base.html.twig' %}

{% block title %}Pendenze - GovPay{% endblock %}

{% block content %}
  <h1>Pendenze</h1>
  <p>Questa è la view di esempio per le pendenze.</p>
  <p>
    <button id="fetch-pendenze" class="it-btn it-btn-secondary">Carica pendenze (demo)</button>
  </p>
  <div class="card">
    <div class="card-body">
      <div id="pendenze-output">{{ debug|raw }}</div>
    </div>
  </div>
{% endblock %}
