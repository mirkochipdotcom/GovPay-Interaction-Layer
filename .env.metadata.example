# Dati che determinano il METADATA SPID/CIE (Service Provider)
#
# Copia questo file in `.env.metadata` (locale, non versionato) e personalizza:
#   cp .env.metadata.example .env.metadata
#
# Nota:
# - Questo file vale sia per SPID sia per CIE (stesso SP, stessi dati organizzazione/policy).
# - L'entityID NON è configurabile: deriva dagli URL statici serviti dal proxy:
#     - ${SPID_PROXY_PUBLIC_BASE_URL}/spid-metadata.xml
#     - ${SPID_PROXY_PUBLIC_BASE_URL}/cie-metadata.xml

# ------------------------------
# Profilo SPID/CIE (metadata/cert)
# ------------------------------
SPID_PROXY_ORG_IS_PUBLIC_ADMIN=1

# Tipo e valore codice organizzazione (per PA: IPACode)
SPID_PROXY_ORG_CODE_TYPE=IPACode
SPID_PROXY_ORG_CODE=c_h501

# Dati organizzazione (consigliato compilarli tutti, e non cambiarli una volta attestato)
SPID_PROXY_ORG_NAME=
SPID_PROXY_ORG_DISPLAY_NAME=
SPID_PROXY_ORG_URL=
SPID_PROXY_ORG_EMAIL=info@organization.org
SPID_PROXY_ORG_PHONE=+3912345678

# Localizzazione (valori richiesti dalla procedura di setup)
SPID_PROXY_ORG_COUNTRY=IT
SPID_PROXY_ORG_LOCALITY=Locality
SPID_PROXY_ORG_MUNICIPALITY=H501
SPID_PROXY_ORG_PROVINCE=RM

# Attributi richiesti nel metadata (lista separata da virgola)
# Nota: il "codice fiscale" dell'utente è tipicamente `fiscalNumber`.
SPID_PROXY_ATTRS=spidCode,name,familyName,fiscalNumber,email

# Nota:
# - le policy runtime del proxy (firma/cifratura/level/ttl/prefix) stanno in `.env.proxyspid`.
