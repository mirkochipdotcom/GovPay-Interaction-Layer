# SPID/CIE - variabili dedicate
# Copia questo file in .env.spid e personalizza:
#   cp .env.spid.example .env.spid
#
# Nota: il file .env.spid viene caricato come env_file SOLO nei servizi:
# - govpay-interaction-frontoffice
# - spid-proxy
#
# Le variabili di Docker Compose (porte, COMPOSE_PROFILES, ecc.) restano in .env.

# -----------------------------
# COME COMPILARE QUESTO FILE
# -----------------------------
# Obiettivo: far sì che il flusso sia coerente tra:
# - Frontoffice: endpoint di callback (dove il proxy POSTa l'esito login)
# - SPID Proxy: lista di redirect autorizzati + base URL pubblico (metadata/entityId)
#
# Minimo indispensabile (DEV):
# 1) Imposta il callback del frontoffice (FRONTOFFICE_PUBLIC_BASE_URL + FRONTOFFICE_SPID_CALLBACK_PATH)
# 2) Assicurati che quel callback compaia in SPID_PROXY_REDIRECT_URIS (match esatto)
# 3) Imposta SPID_PROXY_PUBLIC_BASE_URL coerente con dove raggiungi il proxy dall'esterno (o dal browser)

# Nota su "dove torno dopo il login":
# Il proxy decide dove inviare l'esito *in base al parametro* `redirect_uri` presente nell'URL di /proxy-home.php.
# Quindi se chiami il proxy così:
#   https://login.example.it/proxy-home.php?...&redirect_uri=%2Fproxy-sample.php
# dopo il login tornerai a /proxy-sample.php.
# Per far tornare l'utente al tuo applicativo, devi passare come redirect_uri la tua callback (URL assoluto)
# e tale URL deve essere presente in SPID_PROXY_REDIRECT_URIS.

# ------------------------------------------------------------
# VALIDAZIONE METADATA (IMPORTANTE)
# ------------------------------------------------------------
# Questo progetto genera metadata da Service Provider (SP) SPID “standard” (pubblico o privato).
# Non è pensato per Aggregatori: se nel validator selezioni un profilo aggregatore (es. "pub-ag-full"),
# otterrai errori su campi/estensioni "aggregator/aggregated" che NON devono essere presenti per uno SP.
#
# Quindi, nel validator scegli un profilo di tipo:
# - Service Provider PUBBLICO se nel metadata vedi l'estensione <spid:Public/>
# - Service Provider PRIVATO se nel metadata vedi l'estensione <spid:Private/>
#
# Esempio tipico DEV in locale:
# - frontoffice: https://localhost:8444
# - proxy:       https://localhost:8445
#
#   FRONTOFFICE_PUBLIC_BASE_URL=https://localhost:8444
#   FRONTOFFICE_SPID_CALLBACK_PATH=/spid/callback
#   SPID_PROXY_PUBLIC_BASE_URL=https://localhost:8445
#   SPID_PROXY_REDIRECT_URIS=https://localhost:8444/spid/callback,/proxy-sample.php
#
# Se invece pubblichi il proxy su un dominio (reverse proxy / DNS), usa quel dominio:
#   SPID_PROXY_PUBLIC_BASE_URL=https://login.example.it
#   (e lascia comunque nei redirect il callback reale del frontoffice)

# ------------------------------------------------------------
# GESTIONE METADATA IN PRODUZIONE (LOCK + ROTAZIONE)
# ------------------------------------------------------------
# Scenario reale (AgID):
# - dopo la generazione, il metadata va inviato ad AgID per attestazione;
# - una volta attestato, NON deve cambiare fino alla sua naturale scadenza;
# - poco prima della scadenza si genera un nuovo metadata, senza toccare quello in produzione.
#
# Questo repo supporta tre concetti (da usare insieme):
#
# 1) LOCK (produzione): blocca gli aggiornamenti automatici ai file che influenzano metadata.
#    Obiettivo: evitare che una modifica in .env/.env.spid (o un rebuild) rigeneri setup/cert/metadata.
#    Imposta in PRODUZIONE sul servizio `spid-proxy`:
#      SPID_PROXY_METADATA_MODE=locked
#
# 2) METADATA FROZEN (file statico): Apache, se trova un file statico, lo serve come metadata
#    “ufficiale” dello SP:
#      - path nel container:   /var/www/spid-cie-php/www/metadata/spid-metadata-current.xml
#      - path nel repo (volume): spid-proxy/data/www/metadata/spid-metadata-current.xml
#      - URL pubblico:         ${SPID_PROXY_PUBLIC_BASE_URL}/spid-metadata.xml
#
#    Quindi, dopo che AgID ti attesta il metadata, devi “congelarlo” copiandolo in quel path.
#    Da quel momento il metadata servito resta immutabile (finché non sostituisci il file).
#
# 3) ROTAZIONE (next in parallelo): per generare un metadata "next" SENZA toccare quello in
#    produzione, usa il servizio compose `spid-proxy-metadata` (profilo `spid-proxy-metadata`).
#    Questo servizio lavora su una directory separata (`spid-proxy/metadata-work/`) proprio per
#    non modificare i dati persistenti del proxy “live”.
#
#    Esempio (host):
#      docker compose --profile spid-proxy-metadata run --rm spid-proxy-metadata
#      -> file generato: spid-proxy/metadata-work/www/metadata/spid-metadata-next.xml
#
# Procedura operativa consigliata (PROD):
# A) Dopo attestazione iniziale:
#    - copia il metadata attestato in: spid-proxy/data/www/metadata/spid-metadata-current.xml
#    - imposta: SPID_PROXY_METADATA_MODE=locked
#    - ricrea SOLO il container proxy: docker compose up -d --force-recreate spid-proxy
#
# B) Prima della scadenza (rotazione):
#    - esegui il generator `spid-proxy-metadata` per produrre `spid-metadata-next.xml`
#    - invia `spid-metadata-next.xml` ad AgID per una nuova attestazione
#
# C) Cutover (quando AgID attesta il nuovo metadata):
#    - fai backup del current (es. rinomina in spid-metadata-current.YYYYMMDD.xml)
#    - sostituisci `spid-metadata-current.xml` con il nuovo attestato
#    - ricrea `spid-proxy` (anche solo restart, ma meglio force-recreate per rileggere env)

# Modalità metadata del proxy in produzione:
# - mutable: aggiorna automaticamente setup/config in base alle env (default, utile in dev)
# - locked:  non modifica automaticamente i file che influenzano metadata
SPID_PROXY_METADATA_MODE=mutable

# (opzionale) Snapshot metadata al boot del container (scrive file versionato in www/metadata/).
# NOTA: lascialo a 0 sul servizio `spid-proxy` in produzione.
# Utile solo in modalità "generator" (servizio `spid-proxy-metadata`) o in ambienti controllati.
SPID_PROXY_METADATA_SNAPSHOT_ON_START=0

# (opzionale) Se 1, oltre allo snapshot pubblica anche un file stabile:
# - target=current -> spid-metadata-current.xml (servito su /spid-metadata.xml)
# - target=next    -> spid-metadata-next.xml (servito su /spid-metadata-next.xml)
# NOTA: in produzione il `current` va gestito in modo MANUALE (file attestato), non auto-rigenerato.
SPID_PROXY_METADATA_PUBLISH_ON_START=0
SPID_PROXY_METADATA_PUBLISH_TARGET=current

# (opzionale) Se 1, esegue solo bootstrap/snapshot e poi esce (non avvia Apache foreground).
# Usato dal servizio compose `spid-proxy-metadata`.
SPID_PROXY_GENERATE_ONLY=0

# ------------------------------------------------------------
# FRONT OFFICE (client che avvia il login e riceve la callback)
# ------------------------------------------------------------
# Frontoffice - URL pubblico (usato per comporre il callback SPID).
# Se lasci vuoto, il frontoffice prova a dedurlo da Host/HTTPS della richiesta.
# Esempio: https://localhost:8444
FRONTOFFICE_PUBLIC_BASE_URL=https://localhost:8444

# Path della callback sul frontoffice.
# Esempio: /spid/callback
FRONTOFFICE_SPID_CALLBACK_PATH=/spid/callback

# (opzionale) Redirect URI esplicito per SPID.
# Se lo imposti, deve essere presente anche in SPID_PROXY_REDIRECT_URIS (match esatto).
# Se NON lo imposti, il frontoffice usa di norma:
#   ${FRONTOFFICE_PUBLIC_BASE_URL}${FRONTOFFICE_SPID_CALLBACK_PATH}
# Esempio: https://localhost:8444/spid/callback
FRONTOFFICE_SPID_REDIRECT_URI=

# ------------------------------
# SPID PROXY (interno/esterno)
# ------------------------------
# SPID Proxy (interno o esterno)
# Base URL pubblico del proxy (usato per i link nei metadata e per i redirect HTTP->HTTPS).
# Esempi:
#  - https://login.my-domain.it/
#  - https://localhost:8445   (DEV in locale con certificato self-signed)
SPID_PROXY_PUBLIC_BASE_URL=https://localhost:8445

# EntityID del Proxy SP (di default: ${SPID_PROXY_PUBLIC_BASE_URL}/spid-metadata.xml)
# Lascia vuoto per usare il default.
# Esempio: https://login.example.it/spid-metadata.xml
SPID_PROXY_ENTITY_ID=

# Configurazione client applicativo che usa il proxy (es. GovPay Frontoffice)
# redirect_uri: lista separata da virgola.
# - Può contenere URL assoluti (consigliato, soprattutto se frontoffice ha un dominio pubblico)
# - Può contenere path relativi (es. /proxy-sample.php) che il proxy risolve su SPID_PROXY_PUBLIC_BASE_URL
# IMPORTANTE: deve includere il callback del frontoffice (match esatto), altrimenti il proxy rifiuta il redirect.
# Esempio:
#   SPID_PROXY_REDIRECT_URIS=https://localhost:8444/spid/callback,/proxy-sample.php

# Identificativo del client applicativo.
# Esempio: govpay_frontoffice
SPID_PROXY_CLIENT_ID=govpay_client

# (opzionale) Se vuoi cifrare la response via proxy, imposta anche il secret del client.
# Esempio: change_me_very_secret
SPID_PROXY_CLIENT_SECRET=

# Redirect autorizzati per il client (lista separata da virgola).
# - Metti SEMPRE il callback del frontoffice come URL assoluto (consigliato).
# - Puoi aggiungere path relativi (es. /proxy-sample.php) per pagine demo sul proxy.
# Esempio:
#   SPID_PROXY_REDIRECT_URIS=https://localhost:8444/spid/callback,/proxy-sample.php
SPID_PROXY_REDIRECT_URIS=/proxy-sample.php

# ------------------------------
# BRANDING UI PROXY (header login)
# ------------------------------
# Questi valori controllano il logo e il titolo mostrati nella pagina del proxy (proxy-home.php).
# Se non li imposti:
# - il titolo usa SPID_PROXY_ORG_DISPLAY_NAME (o SPID_PROXY_ORG_NAME / APP_ENTITY_NAME)
# - il logo prova a usare ${FRONTOFFICE_PUBLIC_BASE_URL}/img/stemma_ente.png, altrimenti /assets/img/logo.png

# Nota operativa:
# - Per cambiare SOLO nome/logo della UI, NON serve rigenerare metadata/cert.
# - È sufficiente ricreare il container spid-proxy (es. `docker compose up -d --force-recreate spid-proxy`).
#   Questo perché l'entrypoint aggiorna in modo idempotente i file persistiti:
#   - spid-php-proxy.json (usato da proxy-home.php)
#   - spid-php-setup.json (proxyConfig)

# Titolo mostrato in alto (sostituisce "Default client")
# Esempio: Comune di Montesilvano
SPID_PROXY_CLIENT_NAME=

# Sottotitolo/descrizione mostrata nella pagina (default: SPID_PROXY_ORG_NAME)
# Esempio: Servizi online - Pagamenti
SPID_PROXY_CLIENT_DESCRIPTION=

# URL (assoluto o path) dell'immagine logo.
# Esempi:
#  - https://localhost:8444/img/stemma_ente.png
#  - /assets/img/logo.png
# In produzione usa un URL pubblico raggiungibile dagli utenti (evita 127.0.0.1).
SPID_PROXY_CLIENT_LOGO=

# SPID Proxy - Setup non interattivo (generazione vendor/config/metadata al primo avvio)
# Nota: molte impostazioni vengono “materializzate” su disco al primo avvio (volume del proxy).
# Se modifichi variabili che influenzano metadata/cert/config dopo il primo avvio, potresti dover:
# - eliminare solo il volume/directory dati del proxy e riavviare (NON il DB), oppure
# - eseguire la procedura di setup nel container.

# A cosa serve:
# - È il “baseurlpath” di SimpleSAMLphp (in pratica il prefisso URL con cui vengono esposti i moduli).
# - Con i default di questo repo, i metadata dinamici sono sotto:
#     /myservice/module.php/saml/sp/metadata.php/spid

# Nota importante:
# - È un parametro “avanzato”: cambiarlo modifica le URL dei moduli SimpleSAML (prefisso /<serviceName>/...).
# - L’entrypoint riallinea automaticamente Apache (Alias/rewrite) e la baseurlpath di SimpleSAML, quindi
#   NON dovrebbe “rompere” il proxy solo perché lo cambi.
# - Però, se il volume del proxy è già inizializzato, per applicarlo in modo pulito conviene comunque:
#     SPID_PROXY_FORCE_SETUP=1 + `docker compose up -d --force-recreate spid-proxy`
SPID_PROXY_SERVICE_NAME=myservice

# (manutenzione) Forza rigenerazione setup/metadata al prossimo avvio del container spid-proxy.
# Utile quando cambi variabili come SPID_PROXY_PUBLIC_BASE_URL / SPID_PROXY_ENTITY_ID / ORG_* e vuoi
# rigenerare configurazioni persistite senza cancellare a mano tutto il volume.
# ATTENZIONE: per applicare nuove env ai container esistenti NON basta `docker compose restart`:
# bisogna ricreare il container (es. `docker compose up -d --force-recreate spid-proxy`).
# Nota: è pensato per DEV o per il servizio `spid-proxy-metadata` (generator “next”).
# In PRODUZIONE con `SPID_PROXY_METADATA_MODE=locked` deve restare 0 (l’entrypoint lo ignora comunque).
# Esempio d'uso:
#   SPID_PROXY_FORCE_SETUP=1  (fai un up --force-recreate)
#   poi rimetti SPID_PROXY_FORCE_SETUP=0
SPID_PROXY_FORCE_SETUP=0

# (opzionale) Se 1, oltre al setup elimina anche i certificati SPID in /var/www/spid-cie-php/cert.
# Questo cambia la chiave/certificato del Service Provider: da usare solo se sai cosa stai facendo.
# Tipico uso: generare un metadata “next” con nuova chiave/cert (servizio `spid-proxy-metadata`).
# In PRODUZIONE con metadata congelato, evitare (e comunque viene ignorato se `SPID_PROXY_METADATA_MODE=locked`).
SPID_PROXY_FORCE_CERT=0

# Modalità ambiente del proxy.
# 1 = PRODUZIONE, 0 = SVILUPPO (default).
#
# Cosa cambia in pratica:
# - Se 1 (produzione): il proxy considera NON disponibili alcuni IdP di test/dev
#   (es. DEMO / DEMOVALIDATOR / VALIDATOR / LOCAL), anche se abilitati da altri flag.
#   Obiettivo: evitare di usare per errore IdP “finti” in ambienti reali.
# - Se 0 (sviluppo): puoi usare IdP demo/validator (se abilitati con SPID_PROXY_ADD_*).
# - Influenza anche CIE: in produzione viene usato l'IdP "CIE", in sviluppo "CIE TEST".
#
# Nota operativa (IMPORTANTE): questo valore viene materializzato nel file persistente
# spid-php-proxy.json durante il setup. Quindi per applicare una modifica devi:
#   1) mettere SPID_PROXY_FORCE_SETUP=1
#   2) ricreare il container: docker compose up -d --force-recreate spid-proxy
#   3) rimettere SPID_PROXY_FORCE_SETUP=0
#
# Esempio: 0
SPID_PROXY_PRODUCTION=0

# Abilitazioni
# 1 = abilita SPID, 0 = disabilita.
# Esempio: 1
SPID_PROXY_ADD_SPID=1

# 1 = abilita CIE, 0 = disabilita.
# Esempio: 0
SPID_PROXY_ADD_CIE=0

# IdP preconfigurati (utile in dev)
# In produzione in genere vanno disabilitati (0) e configurati solo gli IdP reali.
SPID_PROXY_ADD_DEMO_IDP=1
SPID_PROXY_ADD_DEMO_VALIDATOR_IDP=1
SPID_PROXY_ADD_AGID_VALIDATOR_IDP=1
# (opzionale) URL metadata IdP locale test
# Esempio: https://idp-test.local/metadata
SPID_PROXY_LOCAL_TEST_IDP_METADATA_URL=

# ------------------------------------------------------------
# PAGINE/FILE DEMO (solo DEV)
# ------------------------------------------------------------
# Questi flag controllano se, durante il setup del container `spid-proxy`, vengono copiati/generati
# dei file PHP di esempio dentro la directory www/ del proxy.
#
# Importante:
# - Sono pensati SOLO per DEV/debug.
# - Se li abiliti una volta, i file scritti su disco (volume) NON vengono automaticamente rimossi
#   quando poi rimetti a 0: per “pulire” devi cancellare i file a mano o re-inizializzare il volume.
#
# 1 = aggiunge l'esempio base `login.php` nella www/ (pagina demo per login SPID).
SPID_PROXY_ADD_EXAMPLES=0

# 1 = aggiunge gli esempi "proxy" nella www/:
# - proxy.php (endpoint proxy)
# - proxy-home.php (UI login del proxy)
# - proxy-sample.php, proxy-login.php, error.php (pagine demo)
# Nota: per usare /proxy-sample.php devi averlo anche in `SPID_PROXY_REDIRECT_URIS`.
SPID_PROXY_ADD_PROXY_EXAMPLE=0

# Attributi richiesti in metadata SPID (lista separata da virgola).
# Nota: questi attributi vengono “materializzati” nei file persistenti durante il setup
# (es. spid-php-setup.json / authsources.php). Se li cambi, applica come per SPID_PROXY_PRODUCTION:
#   SPID_PROXY_FORCE_SETUP=1 + `docker compose up -d --force-recreate spid-proxy`, poi rimetti a 0.
#
# Tutte le possibilità supportate dal setup (spid-cie-php):
# - spidCode
# - name
# - familyName
# - placeOfBirth
# - countyOfBirth
# - dateOfBirth
# - gender
# - companyName
# - registeredOffice
# - fiscalNumber
# - ivaCode
# - idCard
# - expirationDate
# - mobilePhone
# - email
# - address
# - digitalAddress
# - domicileStreetAddress
# - domicilePostalCode
# - domicileMunicipality
# - domicileProvince
# - domicileNation
#
# Esempio “minimo” tipico:
#   spidCode,name,familyName,fiscalNumber,email
SPID_PROXY_ATTRS=spidCode,name,familyName,fiscalNumber,email

# SimpleSAMLphp (admin/secretsalt)
# Password admin di SimpleSAMLphp.
#
# Dove trovi il pannello admin:
# - URL base (frontpage):
#     ${SPID_PROXY_PUBLIC_BASE_URL}/${SPID_PROXY_SERVICE_NAME}/
#   Esempio default:
#     https://localhost:8445/myservice/
# - Alcune pagine tipiche (diagnostica/phpinfo):
#     ${SPID_PROXY_PUBLIC_BASE_URL}/${SPID_PROXY_SERVICE_NAME}/admin/hostnames.php
#     ${SPID_PROXY_PUBLIC_BASE_URL}/${SPID_PROXY_SERVICE_NAME}/admin/phpinfo.php
#
# Come scegliere/generare la password:
# - In DEV puoi lasciare un valore semplice.
# - In ambienti esposti (anche solo rete interna), usa una password lunga e casuale.
#   Suggerimento: >= 20 caratteri con lettere+numeri+simboli.
#
# Come generare un hash (consigliato) con SimpleSAMLphp:
# - Avvia il container (profilo spid-proxy) e poi esegui:
#     docker compose exec spid-proxy php vendor/simplesamlphp/simplesamlphp/bin/pwgen.php
#   Copia il valore generato e mettilo qui. SimpleSAMLphp accetta anche hash.
#
# Nota operativa: questo valore viene scritto nei file persistenti di SimpleSAML.
# Se lo cambi su un proxy già inizializzato, applica con:
#   SPID_PROXY_FORCE_SETUP=1 + docker compose up -d --force-recreate spid-proxy
# poi rimetti SPID_PROXY_FORCE_SETUP=0.
#
# Esempio (DEV): admin
SPID_PROXY_SIMPLESAMLPHP_ADMIN_PASSWORD=admin

# secretsalt di SimpleSAMLphp (usato per hash/token interni).
# Se vuoto viene generato automaticamente al primo avvio.
# In produzione è meglio fissarlo a un valore lungo e casuale.
# Esempio per generarlo (host):
#   openssl rand -hex 32
# Esempio: 9f1c2d3e4a5b6c7d
SPID_PROXY_SIMPLESAMLPHP_SECRETSALT=

# ------------------------------------------------------------
# ORGANIZZAZIONE (PA vs PRIVATI) - metadata/cert
# ------------------------------------------------------------
# Queste variabili finiscono nel metadata SPID e/o nei certificati.
# Compila prima i campi comuni, poi la sezione specifica in base a:
# - SPID_PROXY_ORG_IS_PUBLIC_ADMIN=1 (Pubblica Amministrazione)
# - SPID_PROXY_ORG_IS_PUBLIC_ADMIN=0 (Privato)
#
# Minimo consigliato (per metadata “puliti”):
# - SPID_PROXY_ORG_NAME, SPID_PROXY_ORG_DISPLAY_NAME, SPID_PROXY_ORG_URL
# - SPID_PROXY_ORG_EMAIL, SPID_PROXY_ORG_PHONE

# Ragione sociale / nome ente nel metadata.
# Esempio: Comune di Esempio
SPID_PROXY_ORG_NAME=

# Nome visualizzato (display) nel metadata.
# Esempio: Comune di Esempio
SPID_PROXY_ORG_DISPLAY_NAME=

# Sito web dell'ente/organizzazione.
# Esempio: https://www.comune.esempio.it
SPID_PROXY_ORG_URL=

# Organizzazione: PA o privato
# 1 = Pubblica Amministrazione, 0 = privato
# Esempio: 1
SPID_PROXY_ORG_IS_PUBLIC_ADMIN=1

# ------------------------------
# CODICI ORGANIZZAZIONE (PA/privati)
# ------------------------------

# Codici organizzazione (per PA: IPACode, per privati: VATNumber/FiscalCode)
# Se non valorizzati, fallback su ID_DOMINIO

# Tipo codice organizzazione.
# - PA: IPACode
# - Privato: VATNumber (P.IVA) oppure FiscalCode (CF)
# Esempi: IPACode | VATNumber | FiscalCode
SPID_PROXY_ORG_CODE_TYPE=IPACode

# Valore del codice organizzazione coerente con il type.
# - Se SPID_PROXY_ORG_CODE_TYPE=IPACode: qui va il CODICE IPA dell'ente (es. "c_h501")
# - Se VATNumber: solo numeri P.IVA (es. "01234567890")
# - Se FiscalCode: CF (es. "RSSMRA80A01H501U")
SPID_PROXY_ORG_CODE=c_h501

# Identificativo nel formato atteso dai metadata.
# Per PA, spesso è in forma: PA:IT-<CODICE_FISCALE_ENTE>
# Esempio: PA:IT-01234567890
SPID_PROXY_ORG_IDENTIFIER=

# Codice fiscale dell'organizzazione.
# Esempio: 01234567890
SPID_PROXY_ORG_FISCAL_CODE=

# Codice NACE2 (settore attività). Tipicamente richiesto per privati.
# Esempio: 84.11
SPID_PROXY_ORG_NACE2_CODE=

# ------------------------------
# LOCALIZZAZIONE/CONTATTI (comuni)
# ------------------------------
# Localizzazione (valori richiesti dalla procedura di setup)
# Paese ISO (es. IT)
SPID_PROXY_ORG_COUNTRY=IT

# Località generica (può essere una città)
# Esempio: Montesilvano
SPID_PROXY_ORG_LOCALITY=Locality

# Codice catastale del comune (se applicabile)
# Esempio: H501
SPID_PROXY_ORG_MUNICIPALITY=H501

# Provincia (sigla)
# Esempio: PE
SPID_PROXY_ORG_PROVINCE=RM

# Contatti (dev default). Per prod usa contatti reali.
# Esempio: info@comune.esempio.it
SPID_PROXY_ORG_EMAIL=info@organization.org

# Esempio: +390851234567
SPID_PROXY_ORG_PHONE=+3912345678

# ------------------------------
# PRIVATI (solo se ORG_IS_PUBLIC_ADMIN=0)
# ------------------------------
# Campi FPA (richiesti dal setup quando ORG_IS_PUBLIC_ADMIN=0).
# Nota: compila SOLO se sei privato.

# Paese (ISO) della partita IVA (tipicamente IT)
# Esempio: IT
SPID_PROXY_FPA_ID_PAESE=IT

# Codice/numero di partita IVA (senza prefisso paese)
# Esempio: 01234567890
SPID_PROXY_FPA_ID_CODICE=

# Denominazione azienda
# Esempio: Azienda S.r.l.
SPID_PROXY_FPA_DENOMINAZIONE=

# Indirizzo sede
# Esempio: Via Roma
SPID_PROXY_FPA_INDIRIZZO=

# Civico
# Esempio: 1
SPID_PROXY_FPA_NUMERO_CIVICO=

# CAP
# Esempio: 00100
SPID_PROXY_FPA_CAP=

# Comune
# Esempio: Roma
SPID_PROXY_FPA_COMUNE=

# Provincia (sigla)
# Esempio: RM
SPID_PROXY_FPA_PROVINCIA=RM

# Nazione (ISO)
# Esempio: IT
SPID_PROXY_FPA_NAZIONE=IT

# Nome organizzazione (se richiesto dal profilo privati)
# Esempio: Azienda S.r.l.
SPID_PROXY_FPA_ORG_NAME=
SPID_PROXY_FPA_ORG_EMAIL=info@organization.org
SPID_PROXY_FPA_ORG_PHONE=+3912345678

# Proxy response
# Di default il proxy firma la response. La cifratura (encrypt) richiede tipicamente il secret lato client.

# 1 = firma la response, 0 = no
# Esempio: 1
SPID_PROXY_SIGN_RESPONSE=1

# 1 = cifra la response, 0 = no (richiede di norma SPID_PROXY_CLIENT_SECRET)
# Esempio: 0
SPID_PROXY_ENCRYPT_RESPONSE=0

# Livello SPID richiesto (tipicamente 1/2/3)
# Esempio: 2
SPID_PROXY_LEVEL=2

# Indice attributi/attributi certificati (dipende dal profilo). In dev può restare 0.
# Esempio: 0
SPID_PROXY_ATCS_INDEX=0

# Durata token (secondi)
# Esempio: 1200
SPID_PROXY_TOKEN_EXP_TIME=1200

# Prefisso applicato ai nomi attributo in response (se vuoi namespacing)
# Esempio: spid_
SPID_PROXY_RESPONSE_ATTR_PREFIX=
